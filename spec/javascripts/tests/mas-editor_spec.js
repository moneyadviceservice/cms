describe('MAS Editor', function () {
  'use strict';

  beforeEach(function(done) {
    done();
  });

  afterEach(function() {

  });

  it('should work', function() {
    var bool = false;
    expect(bool).to.be.true;
  });
});
