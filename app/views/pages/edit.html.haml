- content_for :right_column do
  = render 'comfy/admin/cms/sites/mirrors', object: @page

= comfy_form_for @page, as: :page, label_col: 'form__label-heading', control_col: 'form__input-wrapper', url: site_page_path(@site, @page), html: { multipart: true, autocomplete: 'off'}, data: { dough_component: :MASEditor } do |form|
  = render form

= render 'activity_log_box'