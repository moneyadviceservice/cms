$(".page-header").html("<%= escape_javascript(files_page_header(search_term: params[:search])) %>");
$(".search-container").html("<%= escape_javascript(render('search_box', files: @files, search_term: params[:search])) %>");
$(".js-files-listing").html("<%= escape_javascript(render('files_list', site: @site, files: @files, order: @order, type: @type)) %>");
