<% new_html_for_existing_tag_list = @tags.any? ? render(@tags) : content_tag(:span, t('.no_tags', prefix: @prefix)) %>
$(".js-tags-existing").html("<%= escape_javascript(new_html_for_existing_tag_list) %>");
