en:
  activerecord:
    errors:
      models:
        redirect:
          attributes:
            destination:
              identical_to_source: is identical to source
              matches_an_existing_source: must not match an existing redirect source
            source:
              matches_an_existing_destination: must not match an existing redirect destination
              invalid_extension: extension is not supported
